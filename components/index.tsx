import Map, { setAMapKey, setAMapVersion } from './Map';
import Navigate from './Navigate';
import Polygon from './Polygon';
import Marker from './Marker';

export {
  Map,
  Navigate,
  Polygon,
  Marker,
  setAMapKey,
  setAMapVersion,
};

export default {
  Map,
  Navigate,
  Polygon,
  Marker,
  setAMapKey,
  setAMapVersion,
};
